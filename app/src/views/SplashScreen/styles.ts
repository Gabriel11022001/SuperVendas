import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  logo: {
    width: 250,
    height: 250,
    marginBottom: 30
  },
  versaoApp: {
    color: "#fff",
    marginTop: 50,
    marginBottom: 30,
    fontSize: 20,
    fontWeight: "bold"
  }
});

export default styles;