import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

  txtNaoExistemUltimasVendas: {
    color: "#000",
    textAlign: "center",
    marginBottom: 50
  }

});

export default styles;